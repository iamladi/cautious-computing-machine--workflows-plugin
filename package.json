{
  "name": "workflows-plugin",
  "version": "0.1.0",
  "description": "Autonomous workflow orchestration for the full SDLC pipeline",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "bun run scripts/validate-plugin.ts",
    "validate:versions": "bun run scripts/validate-versions.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "execa": "^9.5.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@vitest/coverage-v8": "^3.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "zod": "^3.23.8"
  }
}
